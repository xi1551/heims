<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hospital.mapper.NurseMapper">

    <select id="findByPage" resultType="Nurse">
        SELECT id, job_number, name, gender, title, department_id, phone
        FROM nurse
        <where>
            <if test="name != null and name != ''">
                AND name LIKE CONCAT('%', #{name}, '%')
            </if>
            <if test="departmentId != null">
                AND department_id = #{departmentId}
            </if>
        </where>
        ORDER BY id ASC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="count" resultType="int">
        SELECT COUNT(*)
        FROM nurse
        <where>
            <if test="name != null and name != ''">
                AND name LIKE CONCAT('%', #{name}, '%')
            </if>
            <if test="departmentId != null">
                AND department_id = #{departmentId}
            </if>
        </where>
    </select>

    <select id="findById" parameterType="Long" resultType="Nurse">
        SELECT id, job_number, name, gender, title, department_id, phone
        FROM nurse
        WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="Nurse" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO nurse (job_number, name, gender, title, department_id, phone)
        VALUES (#{jobNumber}, #{name}, #{gender}, #{title}, #{departmentId}, #{phone})
    </insert>

    <update id="update" parameterType="Nurse">
        UPDATE nurse
        <set>
            <if test="jobNumber != null and jobNumber != ''">job_number = #{jobNumber},</if>
            <if test="name != null and name != ''">name = #{name},</if>
            <if test="gender != null">gender = #{gender},</if>
            <if test="title != null">title = #{title},</if>
            <if test="departmentId != null">department_id = #{departmentId},</if>
            <if test="phone != null">phone = #{phone},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="Long">
        DELETE FROM nurse WHERE id = #{id}
    </delete>

</mapper>
