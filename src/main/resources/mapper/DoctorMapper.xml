<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hospital.mapper.DoctorMapper">

    <select id="findByPage" resultType="Doctor">
        SELECT id, job_number, name, gender, title, department_id, phone
        FROM doctor
        <where>
            <if test="name != null and name != ''">
                AND name LIKE CONCAT('%', #{name}, '%')
            </if>
            <if test="departmentId != null">
                AND department_id = #{departmentId}
            </if>
        </where>
        ORDER BY id ASC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="count" resultType="int">
        SELECT COUNT(*)
        FROM doctor
        <where>
            <if test="name != null and name != ''">
                AND name LIKE CONCAT('%', #{name}, '%')
            </if>
            <if test="departmentId != null">
                AND department_id = #{departmentId}
            </if>
        </where>
    </select>

    <select id="findById" parameterType="Long" resultType="Doctor">
        SELECT id, job_number, name, gender, title, department_id, phone
        FROM doctor
        WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="Doctor" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO doctor (job_number, name, gender, title, department_id, phone)
        VALUES (#{jobNumber}, #{name}, #{gender}, #{title}, #{departmentId}, #{phone})
    </insert>

    <update id="update" parameterType="Doctor">
        UPDATE doctor
        <set>
            <if test="jobNumber != null and jobNumber != ''">job_number = #{jobNumber},</if>
            <if test="name != null and name != ''">name = #{name},</if>
            <if test="gender != null">gender = #{gender},</if>
            <if test="title != null">title = #{title},</if>
            <if test="departmentId != null">department_id = #{departmentId},</if>
            <if test="phone != null">phone = #{phone},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="Long">
        DELETE FROM doctor WHERE id = #{id}
    </delete>

</mapper>
